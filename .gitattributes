# Git Attributes file to handle line endings for cross-platform work
# Handle line endings automatically for all text files
* text=auto
# Explicitly set EOL for specific file types
*.sh text eol=lf
data/data_raw/Nat2016us.dat.gz filter=lfs diff=lfs merge=lfs -text
